functions:
	Array<Any> makeArray()
		input
			Any item1
			Any item2
		Array<Any> result
		result.push(item1)
		result.push(item2)
		return result
	Any getFirstElement()
		input
			Array<Any> arr
		return arr[0]

start()
	Array<integer> numbers = makeArray(100, 200)
	print "Array first element: " + numbers[0]
	print "Array second element: " + numbers[1]
	
	Array<string> words = makeArray("hello", "world")
	print "String array first: " + words[0]
	print "String array second: " + words[1]
	
	integer first = getFirstElement(numbers)
	print "First element via function: " + first
	
	string firstWord = getFirstElement(words)
	print "First word via function: " + firstWord