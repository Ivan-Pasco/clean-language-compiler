// Test file for enhanced error recovery
function void test_errors()
	// Missing closing quote
	string message = "Hello world
	
	// Incomplete expression
	integer x = 5 + 
	
	// Missing parenthesis
	print("Test message"
	
	// Wrong function keyword
	func integer add(integer a, integer b)
		return a + b
	
	// Let instead of type
	let y = 10
	
	// Arrow function syntax (not supported)
	integer multiply = (a, b) -> a * b
	
	// Missing closing bracket
	integer[] numbers = [1, 2, 3
	
	// Incomplete if statement
	if x > 0
		print("positive")
	
	print("Test complete") 