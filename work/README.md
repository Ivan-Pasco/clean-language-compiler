# work/

Task briefs whose *Files touched* live in this repository — and only those (`clean-language-foundation EXE-04`). Active briefs sit here; a brief whose acceptance checks pass moves to `work/archive/` in the same change that completes it, with any durable discovery promoted first (`clean-language-foundation DOC-12`). Briefs cite foundation rules as plain-text qualified IDs, resolved against the sibling `../clean-language-foundation` checkout at the commit pinned in CLAUDE.md (rule 3).
