function pairs<string, integer> createPair()
	input
		string key
		integer value
	// For now, just return a simple value since pairs operations aren't implemented yet
	return value

function integer getPairValue()
	input
		pairs<string, integer> pair
	// For now, just return a simple value
	return 42

start()
	pairs<string, integer> myPair
	integer result = getPairValue()
	println:
		"Pairs type test completed" 