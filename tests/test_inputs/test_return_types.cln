functions:  
    string greet(string name)
        return "Hello, " + name + "!"
    
    number calculate(number x, number y)
        return x * y + 10
    
    boolean isEven(number n)
        return n % 2 == 0

start()
    string message = greet("World")
    number result = calculate(5, 3)
    boolean even = isEven(result)
    print message
    print result
    print even 