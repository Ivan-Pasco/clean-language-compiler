start()
	// Test string operations with memory allocation
	string text = "Hello World"
	string sub = text.substring(0, 5)
	print(sub)
	
	string char = text.charAt(6)
	print(char)
	
	string replaced = text.replace("World", "Clean")
	print(replaced)
	
	// Test variable assignment and memory
	string name = "Clean Language"
	integer length = name.length()
	print(length)