start()
	// Test background execution - should queue task, not execute immediately
	background print("Background task executed")
	
	// Test later assignment - should create a future, not execute immediately
	later result = start calculateSum(10, 20)
	
	print("Main thread continues")
	print(result)  // This should access the future result

functions:
	integer calculateSum(integer a, integer b)
		return a + b