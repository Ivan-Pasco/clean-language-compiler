functions:
	list<integer> testListOperations()
		list<integer> numbers = [1, 2, 3]
		List.push(numbers, 4)
		integer last = List.pop(numbers)
		return numbers

start()
	list<integer> result = testListOperations()
	print "List operations test completed"