// Test standard library classes with lowercase, camelCase naming
start()
	print "Testing standard library classes:"
	
	// Test math class
	number sqrtResult = math.sqrt(16.0)
	print "math.sqrt(16.0) = " + sqrtResult.toString()
	
	number sinValue = math.sin(1.57)
	print "math.sin(1.57) = " + sinValue.toString()
	
	number piValue = math.pi()
	print "math.pi() = " + piValue.toString()
	
	// Test string class
	string text = "Hello World"
	integer textLen = string.length(text)
	print "string.length('Hello World') = " + textLen.toString()
	
	string upperText = string.toUpperCase(text)
	print "string.toUpperCase('Hello World') = " + upperText
	
	string trimmed = string.trim("  padded text  ")
	print "string.trim('  padded text  ') = '" + trimmed + "'"
	
	// Test list class
	list<integer> numbers = [1, 2, 3, 4, 5]
	integer listLen = list.length(numbers)
	print "list.length([1,2,3,4,5]) = " + listLen.toString()
	
	integer firstItem = list.get(numbers, 0)
	print "list.get(numbers, 0) = " + firstItem.toString()
	
	list.push(numbers, 6)
	integer newLen = list.length(numbers)
	print "After push(6), length = " + newLen.toString()
	
	print "Standard library classes test completed!"