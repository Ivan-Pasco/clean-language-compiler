start()
	// Test basic list push and pop operations
	list<integer> numbers = [1, 2, 3]
	
	print "Original list length:"
	print List.length(numbers)
	
	// Test push operation
	List.push(numbers, 4)
	List.push(numbers, 5)
	
	print "After push operations:"
	print List.length(numbers)
	
	// Test pop operation
	integer popped = List.pop(numbers)
	print "Popped element:"
	print popped
	
	print "Final list length:"
	print List.length(numbers)