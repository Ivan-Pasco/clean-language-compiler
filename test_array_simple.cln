functions:
	Array<Any> makeArray()
		input
			Any item
		Array<Any> result
		result.push(item)
		result.push(item)
		return result

start()
	Array<integer> numbers = makeArray(42)
	print "Array first element: " + numbers[0]
	print "Array second element: " + numbers[1] 