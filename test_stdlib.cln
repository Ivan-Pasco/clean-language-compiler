start()
	// Math module
	number pi_val = math.pi()
	number sqrt_val = math.sqrt(16.0)
	number sin_val = math.sin(1.57)
	
	// String module  
	string text = "Hello World"
	integer len = string.length(text)
	string upper = string.toUpper(text)
	
	// File module
	boolean exists = file.exists("test.txt")
	
	print pi_val
	print sqrt_val
	print len
	print exists