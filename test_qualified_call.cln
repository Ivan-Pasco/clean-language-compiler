// Test qualified function call instead of method call

import TestModule

start()
    print "Testing qualified function call"
    
    // Try calling the function with qualified name
    integer result = TestModule.add(5, 3)
    print "Result: " + result.toString()