// String Interpolation Test
// Testing the {variable} syntax for embedding expressions in strings

function start()
	string name = "World"
	integer count = 42
	float price = 19.99
	
	// Simple variable interpolation
	string greeting = "Hello, {name}!"
	println(greeting)
	
	// Integer interpolation
	string countMessage = "We have {count} items"
	println(countMessage)
	
	// Float interpolation
	string priceMessage = "Price: ${price}"
	println(priceMessage)
	
	// Property access interpolation
	string user = "John"
	string message = "User {user} has logged in"
	println(message)
	
	// Mixed interpolation
	string summary = "Hello {name}, you have {count} items totaling ${price}"
	println(summary)
end 